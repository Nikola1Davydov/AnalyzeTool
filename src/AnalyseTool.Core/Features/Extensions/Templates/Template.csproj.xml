<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <!-- The Revit year drives everything below: the output folder, the API packages and the runtime.
         Retarget by editing this one number — or without editing at all:
             dotnet build -p:RevitVersion=2026
         (a command-line property overrides this one), so per-year builds stack up side by side.

         The Configuration lines matter for PACKAGING: the SDK's PackExtension target builds one child
         process per year through the configurations "Release R25/R26/R27". A project that only reads
         the property below builds every one of them for the templated year, and packing fails on a
         TargetFramework mismatch. Both routes have to work. -->
    <RevitVersion Condition="$(Configuration.Contains('R27'))">2027</RevitVersion>
    <RevitVersion Condition="'$(RevitVersion)' == '' and $(Configuration.Contains('R26'))">2026</RevitVersion>
    <RevitVersion Condition="'$(RevitVersion)' == '' and $(Configuration.Contains('R25'))">2025</RevitVersion>
    <RevitVersion Condition="'$(RevitVersion)' == ''">__RevitVersion__</RevitVersion>

    <!-- Not a free choice: the Nice3point package for a year is built for that year's runtime, so a
         net8 project referencing the 2027 package fails restore (NU1202). -->
    <TargetFramework Condition="'$(RevitVersion)' &lt; '2027'">net8.0-windows</TargetFramework>
    <TargetFramework Condition="'$(RevitVersion)' &gt;= '2027'">net10.0-windows</TargetFramework>

    <LangVersion>latest</LangVersion>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <AssemblyName>__AssemblyName__</AssemblyName>
    <RootNamespace>__AssemblyName__</RootNamespace>
    <CopyLocalLockFileAssemblies>false</CopyLocalLockFileAssemblies>

    <!-- Build into <extension>\<year>\, the same layout a published package uses — so this folder
         mirrors the package (bin\ and obj\ aside), builds for several Revit years sit side by side
         instead of overwriting each other, and the host resolves the entry assembly year-first
         exactly as it will after install.
         Not $(SolutionDir): no solution is generated here, and outside Visual Studio that property is
         undefined, which silently sent the output to bin\ and left the extension unloadable. -->
    <OutDir>$(MSBuildProjectDirectory)\$(RevitVersion)\</OutDir>
  </PropertyGroup>
  <ItemGroup>
    <!-- AnalyseTool SDK — loaded from the installed plugin. Don't ship a copy. -->
    <Reference Include="AnalyseTool.Sdk">
      <HintPath>__SdkDllPath__</HintPath>
      <Private>false</Private>
    </Reference>
    <!-- Revit API & Newtonsoft are provided by the host at runtime. -->
    <PackageReference Include="Nice3point.Revit.Api.RevitAPI" Version="__RevitVersion__.*">
      <PrivateAssets>all</PrivateAssets>
      <ExcludeAssets>runtime</ExcludeAssets>
    </PackageReference>
    <PackageReference Include="Nice3point.Revit.Api.RevitAPIUI" Version="__RevitVersion__.*">
      <PrivateAssets>all</PrivateAssets>
      <ExcludeAssets>runtime</ExcludeAssets>
    </PackageReference>
    <PackageReference Include="Newtonsoft.Json" Version="13.0.4">
      <PrivateAssets>all</PrivateAssets>
      <ExcludeAssets>runtime</ExcludeAssets>
    </PackageReference>
  </ItemGroup>
</Project>
